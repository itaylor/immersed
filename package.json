{
  "name": "immersed-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Immer based application state management",
  "main": "build/index.js",
  "author": "ian.b.taylor@gmail.com",
  "license": "MIT",
  "dependencies": {
    "immer": "^8.0.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.9",
    "@types/react": "^17.0.0",
    "mocha": "^10.1.0",
    "mocha-ui-jest": "^0.4.0",
    "process": "^0.11.10",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.0"
  },
  "scripts": {
    "build": "yarn workspaces foreach run build",
    "test": "mocha -r mocha-ui-jest -r ts-node/register --ui jest **/*.test.ts",
    "test:e2e": "docker run -it -e NODE_ENV=development -v $PWD:/e2e -w /e2e/todomvc --entrypoint bash cypress/included:5.6.0 -c 'yarn test:e2e'"
  },
  "workspaces": [
    "immersed",
    "immersed-react",
    "todomvc"
  ]
}
